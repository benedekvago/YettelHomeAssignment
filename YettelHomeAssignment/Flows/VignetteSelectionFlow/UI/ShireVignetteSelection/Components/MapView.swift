//
//  MapView.swift
//  YettelHomeAssignment
//
//  Created by Vágó Benedek on 2026. 01. 09..
//

import SwiftUI

/*struct MapView: View {
    let pieces: [ShireImage]

    var body: some View {
        ZStack(alignment: .topLeading) {
            ForEach(pieces.indices, id: \.self) { i in
                Image(uiImage: pieces[i].image)
                    .resizable()
                    .frame(
                        width: pieces[i].frame.width,
                        height: pieces[i].frame.height
                    )
                    .position(
                        x: pieces[i].frame.midX,
                        y: pieces[i].frame.midY
                    )
            }
        }
    }
}*/
